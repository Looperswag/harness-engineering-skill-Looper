# Planner-Generator-Evaluator 工作流详解

这份文档详细展开 Layer 3 的设计原则。这一层是三层框架里最复杂、也最容易过度设计的一层。在动手搭工作流之前,我建议你先读完第一节——很多时候你会发现根本不需要这套东西,省下的时间可以去做更重要的事。

## 第一节:你真的需要分角色工作流吗?

在跳进 Planner、Generator、Evaluator 这些角色定义之前,先冷静地问自己几个问题。

**第一个问题:用户提交的请求里,有多少比例是"真的需要规划多步"的?**

如果绝大多数请求都能通过单步推理 + 一两次工具调用解决,分角色工作流的价值就有限。比如一个"问答型"Agent,用户问什么它答什么,中间最多查一下知识库——这种场景搭三角色工作流是杀鸡用牛刀。

**第二个问题:你设想中的 Evaluator 是在做"实质判断"还是"格式校验"?**

很多人想加 Evaluator 是为了"检查 Generator 的输出对不对",但仔细想想,这个"对不对"其实是在检查 JSON schema、字段是否齐全、数值是否在合法范围——这些事情用确定性代码做更稳定、更便宜、还不会有幻觉。把它包装成 LLM Evaluator 反而引入了新的不确定性。

只有当你的"评估"涉及到"这个修复合不合理""这个回复够不够礼貌""这个解决方案是否针对了真正的根本原因"——这些没有标准答案、需要软性判断的事情时,Evaluator 才有 LLM 的位置。

**第三个问题:你系统的真正瓶颈是什么?**

如果诚实地回顾过去一两个月里 Agent 翻车的案例,把它们归类,你会发现绝大多数翻车都属于"单步质量不稳"——比如 Query 写错了、工具参数填错了、关键信息漏读了。这些问题再加一个 Planner 也不会解决,因为 Planner 出的 plan 还是要靠那个不稳定的单步去执行。

真正能从分角色工作流里获益的场景是:**任务本身需要几步推理 + 中间结果需要根据情况调整路径 + 最终结果有明确的成功/失败信号**。

我的经验性判断:**大多数项目从"单角色 + 强约束 prompt + 工具调用"升级到"双角色 Planner + Generator + 结构化输出"就能解决 80% 的问题。** 再往上加 Evaluator 通常是过度工程,投入产出比不高。

## 第二节:从单角色到双角色的升级

单角色就是大家最熟悉的形态:用户给 prompt,模型直接产出答案,中间可能调几次工具。这种形态对简单任务足够好,但任务稍微复杂一点(比如需要先理解需求、再制定方案、再执行、最后验证),单角色模型经常表现得"急躁"——它会跳过理解和规划阶段直接开始执行,导致执行方向就是错的。

双角色的核心改进是**强制把"思考"和"行动"分开**。Planner 只负责思考和规划,绝对不允许它执行任何具体操作;Generator 只负责执行,绝对不允许它质疑或修改 Plan。这种角色分离听起来人为,但效果非常好,因为它解决了模型"急于动手"的本能问题。

具体怎么落地?最朴素的形态甚至不需要两个独立模型实例,在同一个对话里通过 system prompt 切换角色就行。但要注意一个细节:**两次模型调用之间必须有明确的"交接物"**。Planner 的输出不能是流水账自然语言,要是结构化的 plan(JSON 格式或明确的步骤列表),Generator 接到这个交接物后才开始执行。

为什么强调结构化交接物?因为它强制 Planner 把规划完成度做到位。如果允许自然语言交接,Planner 可能会写"先做 A,然后看情况再说"——这种含糊的 plan 把决策推给了 Generator,角色分离就失败了。结构化交接物迫使 Planner 在规划阶段就把所有该想清楚的都想清楚。

## 第三节:强模型 vs 小模型的根本差异

如果你用的是强模型(Claude Opus 系列、GPT-5 系列、Gemini 3 Pro 这类),双角色甚至三角色工作流相对容易跑通。强模型有几个关键的"自带能力":能在 system prompt 里被指令稳定切角色;能理解自然语言 plan 并精确执行;能做相对开放的评估。

但如果你用的是小模型(30B 规模或更小),上面那套方案会全面崩溃。**核心原因是小模型缺少强模型那几个"自带能力",必须用工程手段补上。**

具体差异和补救措施有五条。

**第一,不要让同一模型切角色,要用独立模型实例。**

强模型可以稳定切角色,小模型不行——它经常串戏。Planner 角色里突然开始执行,Generator 角色里突然评估自己,Evaluator 角色里又给出新的执行方案。这不是"提醒它一下角色"就能解决的,是模型规模本身的限制。

补救方法是**让每个角色用独立的模型实例**,Planner 实例的 system prompt 里只讲规划,完全不提执行;Generator 实例只讲执行,不提规划。三个实例可以共享同一份模型权重,只是 prompt 不同,GPU 占用是可控的。

**第二,不要用自然语言传递信息,要用结构化输出。**

强模型能理解自然语言 plan 并精确执行,小模型经常跑偏——Planner 写的步骤 1-2-3,Generator 跳着做或漏做。

补救方法是**强制结构化输出**。举个例子,Planner 不应该输出"先读用户配置,然后根据配置查询数据库",而应该输出:

```json
{
  "steps": [
    {"id": 1, "action": "read_config", "params": {"path": "..."}},
    {"id": 2, "action": "query_db", "params": {"table": "...", "filter": "from step 1.user_id"}}
  ]
}
```

这种格式让 Generator 解析时不会出错。**有个反直觉的点:小模型在自由文本上飘忽,但在严格的 JSON schema 约束下反而更稳定。约束越强,小模型的输出质量越好。**

**第三,不要让 Planner 一次规划太多步。**

强模型可以一次性 plan 出 10 步;小模型超过 3-4 步就开始飘——后面的步骤往往跟前面的步骤逻辑脱节,或忘记了任务的整体目标。

补救方法是**短链路 plan + 渐进式执行**。Planner 每次只规划接下来的 1-2 步,执行完之后再让 Planner 看新状态、规划下一批。代价是 LLM 调用次数变多,但单次失败的影响也变小了。

**第四,不要让小模型做开放式评估。**

强模型的 Evaluator 可以做"这个输出质量好不好"这种开放式判断。小模型在这种判断上极不稳定——同一个输出问它两次,可能得到截然相反的判断。

补救方法是**把评估拆成多个明确的二元判断,用代码聚合**。

```
不要让 Evaluator 做这种开放式判断:
  "判断这个输出是否优秀"

改成多个明确的二元问题:
  "输出是否包含必填字段 X?(yes/no)"
  "输出格式是否符合 schema?(yes/no)"
  "输出长度是否在 [50, 500] 字符之间?(yes/no)"
```

每个二元问题对小模型来说都是简单任务,准确率很高。最后用确定性代码聚合(全部 yes 才算通过)。

**第五,必须保留强模型/人工的逃生口。**

小模型再怎么优化,在边缘 case 上始终有上限。一个稳健的工程做法是:**80% 的简单 case 全程小模型,15% 的中等 case 小模型 plan + 强模型兜底,5% 的复杂 case 直接路由到强模型或人工**。

这种分级路由的关键是"路由判断"——什么样的请求该升级。最简单的实现是写一个轻量分类器,稍复杂的实现是基于 query embedding 做相似度检索。

## 第四节:工作流的反模式

最后列几个常见反模式,提醒你避开。

**反模式一:为了用而用的多角色。** 看到别人项目里有三角色就觉得自己也得搭一套——其实你的场景可能根本用不上。多角色的工程复杂度是非线性增长的,涉及到角色间通信、状态管理、错误恢复一系列问题。**能用单角色解决的别上双角色,能用双角色解决的别上三角色。**

**反模式二:Evaluator 跟 Generator 用同一个模型。** 这种设计的问题是 Evaluator 会有"自我确认偏误"——它倾向于认为 Generator 的输出是合理的。理想情况下 Evaluator 应该用不同的模型,或至少用不同的 system prompt 让它进入"挑刺模式"。

**反模式三:工作流里没有降级路径。** 设计了 Generator-Evaluator 循环但没设置最大迭代次数,也没有"超过 N 轮就升级处理"的逃生口。结果模型陷入死循环,token 消耗暴涨,问题没解决但账单上去了。**任何工作流都必须有清晰的"终止条件"和"失败兜底"**。

**反模式四:角色 prompt 写得太长。** 三个角色每个都给一个上千 token 的 system prompt,加起来 context 占用极大,留给实际任务的预算就少了。每个角色的 system prompt 应该极致聚焦,核心规则不超过几百 token。

**反模式五:用工作流掩盖真正的问题。** 如果发现 Agent 在某个具体步骤老是出错,正确做法是去优化那个步骤的 prompt、工具或上下文,而不是套一个工作流"让它多试几次"。**工作流不是质量问题的万能解药,它只是把问题分解的工具——分解之后还是要解决根本问题。**

## AVOID 列表

```
AVOID 不评估真实需求就直接搭三角色工作流,80% 的项目双角色就够
AVOID 用强模型方案套小模型项目,小模型必须做独立实例 + 结构化输出 + 短链路规划
AVOID 同一模型在不同角色间切换 system prompt(小模型容易串戏)
AVOID 用自然语言在角色间传递 plan,要用 JSON / Tool calls
AVOID 让小模型一次规划多步,要 plan 1-2 步、渐进式执行
AVOID 让 Evaluator 做开放式判断,要拆成多个二元判断 + 代码聚合
AVOID 没有路由分级机制就上小模型工作流,边缘 case 必须有兜底
AVOID 一上来就搭完整三层方案,要从单角色开始、按需要逐层加
AVOID 工作流里没有最大迭代次数,会陷入死循环
AVOID Evaluator 跟 Generator 用同一个模型,会有自我确认偏误
```

## 简化的决策路径

如果不想读完整,记住下面这个简化决策路径就够了。

**第一步**,先用单角色 + 强约束 prompt + 工具调用试一下。这能解决 60% 的场景。

**第二步**,如果发现 Agent "急于动手没想清楚",升级到双角色 Plan-and-Execute,强制把思考和执行分开。这能再解决 20% 的场景。

**第三步**,如果发现质量问题需要"语义层面的判断"才能识别,加 Evaluator,但要严格遵守"二元判断 + 循环上限 + 降级兜底"三条铁律。剩下 10% 左右场景在这一步搞定。

**第四步**,剩下 10% 以下的极端场景再考虑层级规划、对抗式、自反思等高级模式。

走到第四步的项目其实很少。大多数 AI 协作项目在第二步就能取得很好的工程效果——这也是为什么我反复强调"双角色搞定 80% 的问题"。
