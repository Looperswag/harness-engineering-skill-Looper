# 实施 Checklist:从零开始搭一个 Agent 项目的 Harness

这份文档是整套方法论的"操作手册"。前面三份辅助文档讲的是"为什么这么做"和"具体怎么做",这份讲的是"按什么顺序做"。

我把它分成四个阶段,可以按阶段执行,每个阶段完成后停下来 review,确认稳定再进入下一阶段。**这种渐进式实施能避免"一上来就过度设计"——这是新手最容易犯的错误。**

## 阶段零:动手之前的诊断

在搭任何 harness 之前,先花十分钟回答下面几个问题。这个诊断会直接决定后面三个阶段怎么走。

**第一,这个项目里 Agent 主要做什么类型的任务?**

把它具体到一两句话。常见的几类:辅助编码(写代码、debug、refactor)、内容生成(写文档、生成报告)、数据处理(清洗、分析、报表)、信息检索(搜索、总结、问答)。不同类型的任务,harness 重点完全不同——辅助编码项目重点在 AGENTS.md 的代码约定,数据处理项目重点在工具权限的数据访问边界。

**第二,Agent 会接触哪些"不可逆操作"?**

回想一下 Agent 在你的工作流里可能会调用什么工具、做什么操作。把它们按"如果做错了,我能多快回滚?"分类。如果几乎所有操作都能通过 git 回滚,你需要的 harness 就比较轻量;如果有 deploy、发邮件、改外部数据这种不可逆操作,工具权限设计就是必做项。

**第三,这个项目是个人用还是团队用?**

个人项目可以接受"软约束"(写在 AGENTS.md 里靠模型自觉),团队项目必须有"硬约束"(工具层面强制),因为你不能假设所有团队成员都跟你一样小心。

**第四,你打算用什么模型?**

强模型(Claude Opus 系列、GPT-5、Gemini 3 Pro 这种)和小模型(30B 以下)在 harness 设计上有显著差异。如果用小模型,要特别注意工作流和结构化输出的设计。

## 阶段一:写 AGENTS.md 的最小可用版本

**这一阶段是必须做的,且应该是第一步。** 投入产出比最高——一个小时的工作可能省掉后续几十个小时的沟通和重复纠错。

最小可用版本至少要包含三类内容:

**Project Context** — 极简地说一下"这是什么类型的项目、用了什么技术栈、有什么架构特点"。控制在 50-100 字。

**Build & Test** — 把"怎么构建、怎么测试、怎么 lint"这些命令写出来。必须是可以直接复制粘贴执行的——不要写成抽象描述,要写成 `pnpm test` 这种具体命令。

**Don't Do(AVOID 列表)** — 把已经知道的雷区列出来。哪怕只能想到三五条也比没有强,以后慢慢补充。这一节是 EvoMap 论文证明效果最强的一类内容——AVOID 短句的接受度比正面策略描述更高。

可以缓做的部分:Domain Vocabulary、Code Conventions、Workflow 子章节。这些一开始可以不写,等跟 Agent 协作几次、发现因为术语理解错误或风格不对而出错时再回头补。

**判断这一阶段是否做到位:** 把这份 AGENTS.md 给一个完全没接触过项目的 Agent 看,它能不能在几分钟内开始合格地工作。如果能,就够用;如果不能,看它在哪里卡住,补充对应内容。

## 阶段二:工具权限设计(按需)

**判断逻辑:看 Agent 会不会接触不可逆操作。**

如果 Agent 只是在做"读文件、写本地代码、跑本地测试"这类操作,这一阶段可以缓做——这些操作出错了大不了 git 回滚,不会造成持久损失。

如果 Agent 会接触下面任何一类操作,这一阶段就是必做的:

- 调用外部 API(可能影响别人的系统)
- 修改外部数据库或服务的状态
- 发邮件、发消息、发 webhook
- 部署到 staging 或 production
- 删除任何持久化数据
- 提交到 main 分支或发布版本

具体怎么做工具权限设计,详见 `tool-permissions.md`。最简单的入门方式:列出 Agent 需要用的所有工具,按"读取/执行/变更/外部影响/不可逆"五个层级分类,把每一类的处理策略想清楚。

如果用的是 Claude Code、Cursor 这类自带权限配置的工具,把权限规则写在它们的配置文件里。如果是自建 Agent 系统,在工具调用层加一层 allowlist 检查。

**判断这一阶段是否做到位:** 思考"如果 Agent 在我不在场的时候被恶意 prompt 误导,它最坏能造成多大破坏?"——如果答案是"几小时回滚就能恢复",可以接受;如果是"会损失客户、损失数据、损失钱",权限设计还没做到位。

## 阶段三:分角色工作流(按需)

**这一阶段在大多数项目中是不需要做的。**

跳过这一阶段的合理场景:

- Agent 任务是单步或两步以内可完成的(代码补全、单文件修改、回答问题)
- 项目主要瓶颈是单步质量,不是任务规划
- 用的是 Claude Code、Cursor 这类已经内置工作流的工具

只有满足下面所有条件,才值得搭分角色工作流:

- 任务平均需要三步以上才能完成
- 步骤之间有明确的依赖关系
- 中间结果需要根据情况调整后续路径
- 最终结果有可验证的成功/失败信号

满足条件后,**先从最简单的双角色(Planner + Generator)开始,不要一上来就上三角色**。具体设计方法详见 `workflow-patterns.md`。

**特别提醒:用小模型时,工作流实现方式跟强模型完全不同。** 小模型必须用独立模型实例(不要切角色)、必须用结构化输出、必须做短链路 plan。

## 阶段四:持续迭代(必做习惯)

无论前三阶段做得多完善,跟 Agent 实际协作之后总会发现新的翻车场景。**这些翻车背后往往是"项目特有的隐性知识"**——你以为大家都知道的事,Agent 不知道。

每次发现一个翻车场景,做下面这几个动作:

**第一,记录翻车现场。** 包括 Agent 当时收到的指令、输出、错在哪里、应该怎样才对。可以做得简略,关键是不要遗忘。

**第二,问自己"根因是什么"。** 是 Agent 不知道某个事实?不知道某条规则?误解了某个术语?走错了某个流程?根因决定了你要把经验写到哪里。

**第三,蒸馏成 AVOID 短句。** 加到 AGENTS.md 的 Don't Do 列表里。注意:每条 AVOID 必须独立、自包含、针对一个具体错误。

**第四,定期 review。** 每个月或两周,把 AVOID 列表整体看一遍,合并重复的、删除不再相关的。**AVOID 列表不是只增不减的日志,它是动态维护的资产。**

这一阶段背后的方法论来自 EvoMap Gene 论文——失败的最佳沉淀形态不是 trajectory log 也不是 reflection summary,而是蒸馏过的 AVOID 警告。

## 几个常见错误的提前规避

**错误一:第一版 AGENTS.md 写得太完美。** 想一步到位写出最完整的 AGENTS.md,结果花两天写了三百行,大部分还是猜测的规则,实际跑起来发现一半不对。**正确做法是第一版先写五十行、跑起来、根据实际表现迭代。完美主义在这件事上是大敌。**

**错误二:工具权限设计跟 AGENTS.md 不同步。** 在 AGENTS.md 里说"不要 deploy 到 production",但工具配置里没禁止——这种"软约束没硬约束兜底"的设计迟早会出事。**重要约束必须两层都写。**

**错误三:没设循环上限就上 Generator-Evaluator 工作流。** 模型陷入死循环,token 消耗暴涨,问题没解决但账单上去了。任何工作流上线前先确认"终止条件"明确。

**错误四:把工作流当成质量问题的万能解药。** Agent 在某个具体步骤老犯错,你不去优化那个步骤,而是套一个工作流"让它多试几次"——这是回避问题不是解决问题。**工作流是分解问题的工具,不是解决问题的工具。**

**错误五:复制别人的 AGENTS.md 模板原封不动用。** 包括 `AGENTS_template.md` 里给的例子。**模板的价值是给你结构和提示,不是给你最终内容。** 每个项目的 AGENTS.md 都应该是独特的,因为每个项目的"私有词汇""容易踩的坑""特殊约定"都不一样。

## 一份精简的 30 分钟启动方案

如果你只有半小时,想快速给一个新项目搭起最小可用的 harness,按下面这个时间表:

**前 10 分钟做诊断:** 回答阶段零的四个问题,在心里有个项目画像。

**中间 15 分钟写 AGENTS.md:** 从 `AGENTS_template.md` 复制六个核心 section,每个 section 写两三行最关键的内容,总共控制在 50 行以内。**重点放在 Domain Vocabulary 和 Don't Do 这两节——这是收益最大的两节。**

**最后 5 分钟跑验证:** 让 AI 读 AGENTS.md,给它一个小任务跑一下,看行为是否符合预期。如果有明显违规,定位到对应的 section 优化一下。

这个 30 分钟方案能让你在一个新项目上快速建立基础 harness,后续再根据实际使用慢慢加深。它不能解决所有问题,但能解决头几天最常遇到的那批问题——而这就是新项目早期最需要的。
