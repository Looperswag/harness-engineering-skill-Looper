# 工具权限设计

Harness Engineering 的 Layer 2:**用减法定义 Agent 能力边界**。

如果你的 Agent 不会接触不可逆操作(只读文件、跑测试、改本地代码),这份文档大部分内容可以跳过——但末尾的 AVOID 列表建议看一眼。

## 为什么这一层重要

模型大多数时候在做"建设性"工作:读文件、写代码、跑测试。这些操作就算偶尔出错,代价也很低——大不了 git checkout 撤销一下。

但只要 Agent 接触到"破坏性"或"不可逆"的能力,事情就完全不一样了。它发了一封邮件、删了一张表、提交污染了主分支的 commit、动了 production 配置——这些不是"撤销"两个字能解决的。

工具权限设计的核心目的,就是在这两类操作之间画一条清晰的红线,让 Agent 在红线之内自由发挥,在红线之外必须停下来。

李宏毅老师把这件事概括成"用减法定义 Agent"——你不需要列举 Agent 能做什么(它的能力边界本来就是无限的),你需要做的是圈定它不能做什么。

## 五个权限层级

我把 Agent 可能接触的所有操作分成五个层级,每一级的失败成本不同,处理策略也不同。

**第一级:读取类操作**

读文件、查询数据库、列目录、调只读 API、搜索网页。失败成本极低,默认完全开放。

唯一需要小心的是"读取敏感数据"——隐私、密钥、未公开数据应该通过文件系统权限或 .gitignore 隔离开,**而不是依赖 Agent 自己懂得"不该读这个"**。Agent 不该被信任去判断什么是敏感数据,这件事必须由环境本身保证。

**第二级:执行类操作**

跑测试、构建、执行本地脚本、跑数据分析。这些操作消耗资源(CPU、时间),但不直接改变持久状态。失败成本中等。

默认开放,但有两个建议:**总要有超时机制**(避免无限循环占满 CPU);**记录执行历史**(事后审计 Agent 都跑过什么)。

**第三级:变更类操作**

写文件、删文件、创建分支、git commit。一旦改坏一个文件,你需要花时间审查 diff 决定怎么恢复。

不应该完全开放,要按"明确允许的范围"授权。比如允许 Agent 修改 src/ 下的所有 .ts 文件,但禁止动 .github/ 下的 CI 配置;允许创建新分支,但禁止直接 push 到 main。

实现方式有两种:**工具本身的 allowlist 配置**(比如 Claude Code 的权限配置)、**写在 AGENTS.md 里作为约束**让模型自觉遵守。前者是硬约束,后者是软约束,**重要操作两者都做**。

**第四级:外部影响类操作**

调第三方 API、发 HTTP 请求、改外部数据库、发邮件、发 webhook。这一类的失败成本显著上升,因为它影响的是别人的系统。

举个直观的例子。让 Agent 帮你处理一批待办邮件,因为 prompt 没写清楚,它把"草稿"理解成了"立即发送",结果给一百个客户都发了一封半成品邮件——这个事故的处理成本就是几小时善后 + 难以挽回的客户体验损失。

处理原则:**默认需要 confirmation,且 confirmation 必须能描述清楚操作的后果**。"是否调用这个 API"是不够的,应该是"是否给以下 100 个客户发送邮件,标题是 X,正文预览见下方"——让用户能看清楚到底要发生什么。

**第五级:不可逆操作**

deploy 到 production、删生产数据、改 prod 配置、合并到 main、发布版本。这一类是绝对的红线。

强烈建议:**这一类操作不应该让 Agent 直接执行,必须经过人工 approval 这一道**。哪怕 Agent 把所有准备工作都做好了,最后扣下扳机的应该是人。

这不只是技术安全考量,更是责任归属考量——一旦 Agent 自动执行了不可逆操作并出了事,责任谁来承担?所以最稳妥的做法就是不让模型独立做这种事。

## 几个具体的设计动作

**第一,allowlist 而不是 blocklist。** allowlist 的逻辑是"只有列出的操作才允许,其他全部拒绝";blocklist 是"列出的操作禁止,其他全部允许"。allowlist 更好,因为新工具、新 API、新能力总在涌现,blocklist 永远追不上;而 allowlist 只在你明确想新增能力时才扩展,默认就是安全的。

**第二,给操作配范围参数,而不是一刀切的开关。** 不应该简单地"允许 Agent 写文件",而应该"允许 Agent 写 src/ 下的文件,但 src/lib/vendor/ 除外"。范围参数化让权限粒度可控。

**第三,用 dry-run 替代真实执行。** 很多有风险的操作都有 dry-run 模式——SQL 可以先 EXPLAIN、API 调用可以先 sandbox 测试、deploy 可以先 staging。让 Agent 默认走 dry-run 路径,把结果给你看,确认无误再真实执行。

**第四,审计可追溯。** 第三级以上的所有操作都应该被记录——谁触发的、什么时间、操作内容、结果如何。这不只是事后追责,更是迭代优化的输入——审视一周操作记录时,你会发现 Agent 反复犯的错误,这些就是该加进 AVOID 列表的内容。

## 跟 AGENTS.md 的协同

工具权限设计和 AGENTS.md 不是两件独立的事:

- **AGENTS.md 表达"软约束"**——告诉 Agent 应该和不应该做什么,依赖模型自觉遵守
- **工具权限表达"硬约束"**——技术层面强制 Agent 不能做某些事,即使它想做也做不到

最佳实践是两者配合,**重要约束在两个地方都写一遍**。AGENTS.md 里写"Don't directly modify production config files"是给模型的提醒;工具配置里把 production config 路径加入 deny list 是技术兜底。两层叠加,即使一层失效另一层也能挡住。

为什么不能只靠技术兜底?因为有些约束是技术层面无法表达的——"不要在没读完相关测试的情况下就开始改代码"是行为流程的约束,只能写进 AGENTS.md。

为什么不能只靠 AGENTS.md?因为模型偶尔会犯错或被对抗性输入引导。AGENTS.md 提供"统计上 95% 的可靠性",工具权限提供"硬边界 100% 的可靠性"——涉及不可逆操作时,你需要的是后者。

## AVOID 列表

```
AVOID 把"不要做 X"写在 AGENTS.md 里就当作万事大吉,关键操作必须工具层面也阻止
AVOID 用 blocklist 思维设计权限,要用 allowlist——默认拒绝,显式开放
AVOID 给 Agent "全有或全无"的权限粒度,要给细粒度授权(按路径/命名空间/操作类型)
AVOID 让 Agent 自己判断"什么是敏感数据",敏感数据隔离必须由环境保证
AVOID 跳过 dry-run 直接让 Agent 真实执行高风险操作,默认应该走预演路径
AVOID 不记录 Agent 的高权限操作,审计日志是改进 AGENTS.md 的关键输入
AVOID 让 Agent 直接执行不可逆操作(deploy/delete/publish),最后扣扳机的必须是人
AVOID 一次开放过多新权限,新工具、新 API 应该一次只开一个
```

## 简化的决策树

如果你不想读完整套原则,只想要一个快速判断:

**第一步**,列出 Agent 可能用到的所有操作类型,按"如果做错了,我能多快回滚?"分类。

**第二步**,能在五分钟内回滚的操作(读文件、跑测试、改本地代码)→ 默认开放。

**第三步**,需要十分钟到一小时回滚的操作(改 staging 系统、发可撤回的请求)→ 默认开放但要 confirmation 和 audit log。

**第四步**,需要数小时到数天回滚,或根本无法完全回滚的操作(发邮件给客户、改 production 数据、发布版本)→ 不让 Agent 直接执行,必须人工 approval。

这个决策树虽然简单,但能覆盖 90% 的实际场景。复杂场景再回头看完整的五层级框架。
